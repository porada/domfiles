#!/bin/sh

# shellcheck source=bin/domlib
. "$(dirname "$(realpath "$0")")/domlib"

# Show short `git status` output including skipped files
[ "${1-}" = '--' ] && shift

git -c status.relativePaths=false status --short -- "$@" || exit $?
__domfiles_bin git-skipped "$@"
