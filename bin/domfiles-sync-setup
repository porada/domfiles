#!/bin/sh

# shellcheck source=domlib
. "$(dirname "$(realpath "$0")")/domlib"

__print_heading 'Linking domfiles'

__symlink "$DOMFILES_CONFIG_DIR" "$HOME/.config"

for source in .curlrc .vimrc; do
  __symlink "$DOMFILES_CONFIG_DIR/$source" "$HOME/$source"
done

__print_heading_dimmed 'Linking agent instructions'

__symlink "$DOMFILES_ZED_CONFIG_DIR/AGENTS.md" "$HOME/.claude/CLAUDE.md"
__symlink "$DOMFILES_ZED_CONFIG_DIR/AGENTS.md" "$HOME/.codex/AGENTS.md"

__print_heading_dimmed 'Linking agent skills'

for source in agent-task-relay domfiles-agent-documentation domfiles-git-worktrees \
  domfiles-release-notes domfiles-repository-harmonization fish-shell-scripting \
  human-facing-writing posix-shell-scripting release-notes-for-humans simple-github-cli; do
  name="$(__string_replace "$source" 'domfiles-' '')"
  path="$DOMFILES_SKILLS_DIR/$source"

  __suppress __symlink "$path" "$HOME/.agents/skills/$name"
  __suppress __symlink "$path" "$HOME/.claude/skills/$name"
  __print_confirmation "$source"
done

__print_heading_dimmed 'Provisioning additional files'

__mkdir "$DOMFILES_PROJECTS_DIR"
__touch "$DOMFILES_FISH_CONFIG_DIR/local.fish"
__touch "$DOMFILES_GIT_CONFIG_DIR/allowed_signers"
__touch "$DOMFILES_NPM_CONFIG_DIR/user.npmrc"
__touch "$HOME/.hushlogin"

__print_heading_dimmed 'Ensuring proper permissions'

__chmod "$DOMFILES"
__chmod "$HOME/.ssh"

__print_heading_dimmed 'Verifying SSH setup'

if ! __is_ssh_configured; then
  __print_error "SSH setup requires a complete key pair at \`$DOMFILES_SSH_KEY\`"
else
  __print_confirmation "$DOMFILES_SSH_KEY"

  # Set up allowed signers for `git`
  signer="$(git config --global --get user.email 2> /dev/null || true)"

  if [ -z "$signer" ]; then
    __print_error_and_exit 'Failed to resolve `user.email` from `.config/git/config`'
  fi

  key=''
  type=''
  read -r type key _ < "$DOMFILES_SSH_KEY.pub" || true

  if [ -z "$key" ] || [ -z "$type" ]; then
    __print_error_and_exit "Failed to parse the public key at \`$DOMFILES_SSH_KEY.pub\`"
  fi

  printf '%s namespaces="git" %s %s\n' "$signer" "$type" "$key" \
    > "$DOMFILES_GIT_CONFIG_DIR/allowed_signers"

  __print_confirmation "$DOMFILES_GIT_CONFIG_DIR/allowed_signers"
fi
