name: Test

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

permissions:
  contents: read

jobs:
  sync:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v6
      - run: sw_vers
      - run: bin/domfiles-sync
      - run: fish --command 'domfiles sync'
